# Agents

Under no circumstances read `node_modules`.

Do not read generated artifacts or caches, including `.next`, `.open-next`, `.wrangler`,
`dist`, `build`, `out`, `coverage`, `.turbo`, or `.cache`, unless explicitly asked to
debug them.

This project uses the Payload CMS skill at `.agents/skills/payload/`.
Start with `.agents/skills/payload/SKILL.md` for a quick reference, then see `.agents/skills/payload/reference/` for detailed docs.
